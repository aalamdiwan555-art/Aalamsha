# Autopilot

Autopilot is an Android driver-assistance app with package name
`com.autopilot.driver`.

The Android application lives in `app/`. The repository also contains the
Replit workspace configuration used to develop and automate the project.
Android builds use the Gradle wrapper and the GitHub Actions workflow in
`.github/workflows/build-apk.yml`.